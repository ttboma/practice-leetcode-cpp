/out

# The reason we don't ignore /.vscode is that we want to keep our VsCode settings
# and configurations for future reference.
#/.vscode

/.venv

# Symlink to compile_commands.json (generated from CMake)
/compile_commands.json
