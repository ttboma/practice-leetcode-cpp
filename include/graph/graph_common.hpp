#pragma once

namespace graph {

// BGL-style color map for tracking vertex states
// Shared definition used by both BFS and DFS algorithms
enum class Color { White, Gray, Black };

}  // namespace graph
